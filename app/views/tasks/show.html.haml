%p#notice= notice

%p
  %b Title:
  = @task.title
%p
  %b Date start:
  = @task.date_start
%p
  %b Date end:
  = @task.date_end
%p
  %b State:
  = @task.human_state

= link_to 'Edit', edit_project_task_path(@project, @task), class: 'btn btn-outline-primary mt-2'
\|
= link_to 'Back', project_tasks_path(@project), class: 'btn btn-outline-info ml-2 mt-2'