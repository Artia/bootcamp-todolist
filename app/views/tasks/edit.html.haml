%h1 Editing task

= render 'form'

= link_to 'Show', project_task_path(@project, @task), class: 'btn btn-outline-secondary mt-2'
\|
= link_to 'Back', project_tasks_path(@project, @task), class: 'btn btn-outline-info ml-2 mt-2'
